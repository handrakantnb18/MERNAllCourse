// The Arrow function  =>

    const say = () =>{
        console.log("Hello, Arrow function");
    }
    say();

    // addition of two numbers
    const addition = (a, b) => {
        return a + b;
    }
    console.log("Addition of a and b : ", addition(25, 75));

    // multiplication of two numbers
    const multi = (index1, index2) => index1 * index2;
    console.log("Multiplication of two index : ", multi(5, 6));

