// Implement the replaceall function

// .replaceAll() -->  method replaces a string instead of just the first one

const rep = "Hello world, world is javascript";
console.log(rep.replaceAll(`Hello`,`javascript`));

console.log(rep.replaceAll(`javascript`,`Hello`));
